{
    "name": "fullstack-app",
    "private": true,
    "scripts": {
        "install-all": "npm-run-all install:backend install:frontend",
        "install:backend": "npm --prefix backend install",
        "install:frontend": "npm --prefix frontend install",
        "build-frontend": "npm --prefix frontend install && npm --prefix frontend run build",
        "build-frontend-prod": "npm --prefix frontend install && cross-env VITE_SUPABASE_URL=%VITE_SUPABASE_URL% VITE_SUPABASE_ANON_KEY=%VITE_SUPABASE_ANON_KEY% npm --prefix frontend run build",
        "start": "npm --prefix backend start"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "npm-run-all": "^4.1.5",
        "supertest": "^7.1.4",
        "vitest": "^4.0.15"
    }
}
